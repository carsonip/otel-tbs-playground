receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:44317"

processors:
  batch:
    send_batch_size: 100000000
    timeout: 100s

connectors:
  signaltometrics:
    spans:
      - name: span.count
        description: Count of spans
        sum:
          value: "1"

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [signaltometrics]
    metrics:
      receivers: [signaltometrics]
      exporters: [debug]
  telemetry:
    metrics:
      readers:
        - pull:
            exporter:
              prometheus:
                host: localhost
                port: 8889